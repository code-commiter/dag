query = {
                "state": "QUEUED",
                "type": {"$ne": "SCHEDULER"}, # Exclude SCHEDULER tasks, as they are managed by reflow_scheduler_monitor_dag
                "$or": [
                    {"scheduledTime": None},
                    {"scheduledTime": {"$lte": now_utc}}
                ]
            }
